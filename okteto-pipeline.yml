deploy:
  - okteto build --file api.Dockerfile -t tomaszczerminski/api:${OKTETO_GIT_COMMIT} api
  - okteto build --file domain.Dockerfile -t tomaszczerminski/domain:${OKTETO_GIT_COMMIT} domain
  - helm upgrade --install octopus chart --set tag=${OKTETO_GIT_COMMIT}